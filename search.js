const db = require('./db');

module.exports = async (query) => {
	
	const date = query.date;
	const iataTo = query.to;
	const iataFrom = query.from;
	//const first_class = query.from;

	//await db.basic_query('')

}